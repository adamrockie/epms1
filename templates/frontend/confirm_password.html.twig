{% extends "frontend/patials/master.html" %}

{% block title %} Register {% endblock %}

{% block head %}  {% endblock %}

{% block content %}

<div class="card">
    <div class="row align-items-center text-center">
        <div class="col-md-12">
            <div class="card-body">
                <form class="" action="post_password" method="POST">

                <img src="assets/images/logo1.png" width="65px" alt="" class="img-fluid mb-4">
                <h4 class="mb-3 f-w-400">Update Account Password</h4>

                <div class="form-group mb-4">
                    <label class="floating-label" for="Password">Password</label>
                    <input type="password" name="password"id="password" class="form-control"  placeholder="" required>
                </div>
                <div class="form-group mb-4">
                    <label class="floating-label" for="Password">Confirm Password</label>
                    <input type="password" name="confirm_password"id="confirm_password" class="form-control"  placeholder="" required>
                </div>

                <input type="hidden" name="token" value="{{token}}"><input type="hidden" name="uid" value="{{uid}}">
                <button type="submit" name="submit" class="btn btn-primary btn-block mb-4">Submit</button>

                </form>
                <p class="mb-2">Already have an account? <a href="login" class="f-w-400">Signin</a></p>
            </div>
        </div>
    </div>
</div>


<script>
    
      $("#confirm_password").blur(
        function check() {
          var password;
          password = $('#password').val();
          c_password = $('#confirm_password').val();
    
          if(password === c_password) {
            
          }else{
            alert('Password and Confirm Password does not match');
            document.getElementById('confirm_password').value='';
          }
        });      
    
    </script>
{% endblock %}